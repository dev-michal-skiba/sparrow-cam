user www-data;
worker_processes auto;
pid /run/nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen ${WEB_PORT};
        server_name _;

        location / {
            root /var/www/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        # Serve HLS streams
        location /hls/ {
            types {
                application/vnd.apple.mpegurl m3u8;
                video/mp2t ts;
            }
            root /var/www/html;
            add_header Cache-Control no-cache;
            add_header Access-Control-Allow-Origin *;
        }

        # Serve processed HLS streams from processor
        location /annotations/ {
            types {
                application/json json;
            }
            root /var/www/html;
            add_header Content-Type application/json;
            add_header Access-Control-Allow-Origin *;
        }

        # Serve archived HLS streams
        location /archive/storage/ {
            alias /var/www/html/storage/sparrow_cam/archive/;
            types {
                application/vnd.apple.mpegurl m3u8;
                video/mp2t ts;
                application/json json;
            }
            add_header Cache-Control no-cache;
            add_header Access-Control-Allow-Origin *;
        }

        # Proxy archive API requests
        location /archive/api {
            proxy_pass ${ARCHIVE_API_URL}/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }

    include /etc/nginx/conf.d/*.conf;
}
