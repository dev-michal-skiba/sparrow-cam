version: '3.8'

services:
  deploy:
    image: sparrow_cam_deploy
    container_name: sparrow_cam_deploy
    volumes:
      - ./ansible:/ansible
      - ../app:/app
    working_dir: /ansible
    environment:
      - ANSIBLE_HOST_KEY_CHECKING=False
